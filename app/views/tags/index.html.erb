<h1>All Tags</h1>

<ul id="tags">
    <% @tags.each do |tag| %>
        <li>
            <%= link_to tag.name, tag_path(tag), class: 'tag_title', id: "tag#{tag.id}"%>
        </li>
    <%end%>
</ul>

<br>
<br>

<%= link_to "<<Back to Articles Overview", articles_path %>
